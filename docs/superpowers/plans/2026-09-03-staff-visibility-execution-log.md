# SDD ledger — plan: docs/superpowers/plans/2026-09-03-staff-visibility.md

Spec: docs/superpowers/specs/2026-09-03-staff-visibility-design.md (read; binding authority)
Branch: spec1-staff-visibility, cut from codex at eb96d55
Baseline suite: 2504 passed / 1 failed (2505). The failure is a known timing flake —
tests/actions/use-server-exports.test.ts times out at 5000ms under full-suite contention,
passes alone in ~3s. Task 6 raises that timeout. Do not treat it as a regression.

## Ruling: branch off `codex` rather than implementing on it

The user works on `codex`, and a second agent (Codex) ships schema there — memory
`codex-branch-parallel-agent`. Task 2 adds a migration; two agents adding migrations to one
branch concurrently produces a conflict that is painful to unpick after the fact.
Cost if wrong: one extra merge at the end, which `finishing-a-development-branch` handles.

## Pre-flight scan

### Shared files across tasks

| File | Tasks | Producer → consumer | Finding |
| --- | --- | --- | --- |
| `src/lib/auth/roles.ts` | T1 creates; T3, T4, T6, T12, T13 consume | `USER_ROLES`, `UserRole`, `DEFAULT_ROLE`, `isKnownRole`, `isStaff`, `isAdmin` | **T1's Interfaces block omits `DEFAULT_ROLE`, which T3 and T12 consume.** Its code block does define it. Ruled below. |
| `src/lib/staff/access.ts` | T4 creates; T6-T12 consume | `requireStaff`, `requireAdmin`, `StaffSession` | Consistent. |
| `src/lib/staff/queries.ts` | T7 creates; T11 appends | `StaffKlpRow`, `StaffCoverageRow`, `StaffOverview`, then `LearnerRecord` | Consistent; T11 appends, does not rewrite. |
| `src/actions/staff.ts` | T7 creates; T11 appends | 3 actions, then a 4th | Consistent. T7's generic gating test auto-covers T11's addition. |
| `tests/actions/klt-gated-exports-guard.test.ts` | T6, T7, T12, T17 | `GATE_PATTERNS` → `FILES` → `FILES` → `READ_GATE_ALLOWLIST` | Four tasks, four disjoint edits, sequential. T6 explicitly defers the `FILES` edit to T7 because a guard reading a not-yet-created file fails. Consistent. |
| `tests/app/staff-pages.test.tsx` | T8 creates; T9-T12 append | mock factory grows per task | Each append adds its own mock. Sequential, so each implementer sees the current state. Coordination point, not a conflict. |
| `src/components/sets/knowledge/ConceptKlps.tsx` | T16 stubs; T17 rewrites | stub → real | Deliberate and labelled in the plan, so T16 is independently testable. |
| `src/lib/metrics/klt-rollup.ts`, `topic-profile.ts` | T14 only | `RawKltRow.parentName` → `TopicRow.parentKey` | Rename happens once, inside `kltRowsToTopicRows`, and is asserted there. |
| `src/lib/sets/knowledge.ts` | T15 only | `TopicMasteryRow.parentKey`/`hasChildren` | T15 must also add both to `shapeTopicMastery`, which its Step 3 says. Consistent. |
| `src/lib/shell/nav.ts` | T13 only | `railItems(signedIn, role)` | Signature change; only caller is `RailNav.tsx`. Verified. |
| `prisma/schema.prisma` | T2 only | `User.role`, `RoleGrant` | No other task touches the schema. |

### Per-task self-consistency

Each task's tests were checked against the code it specifies, and the files it creates against
the files later tasks touch. Two tasks failed; both fixed in the plan before dispatch.

- **T16 — FAILED.** Its test helper built a `TopicMasteryRow` with `shade: 'shaky'`.
  `MasteryShade` is `'unknown' | 'weak' | 'developing' | 'solid' | 'strong'` — no `'shaky'`.
  Would have failed `tsc` on a line the implementer had no reason to doubt.
  **Fixed in plan:** now `'developing'`, with the vocabulary named in a comment.
- **T13 — FAILED.** Said "modify whichever component renders `<RailNav>`", implying one.
  There are two paths: `layout.tsx` → `CollapsibleShell` → `RailNav`, and
  `CollapsibleShell` → `MobileRail` → `RailNav`. Threading `role` down only the first leaves
  the staff link missing on mobile — a bug that passes every unit test.
  **Fixed in plan:** all four files named with line numbers, and the mobile hop called out.
- T1-T12, T14, T15, T17, T18 — checked, self-consistent.

### Ruling: T1's Interfaces block omits `DEFAULT_ROLE`

T3 and T12 consume it and T1's code block defines it, so an implementer transcribing the brief
produces it. Not worth a plan edit; noting it here so a later fix round does not treat its
presence as scope creep. Cost if wrong: T3's implementer adds a one-line export.

### Ruling: no other task may edit `prisma/schema.prisma`

T2 owns the schema. If a later task believes it needs a column, it stops and reports rather than
migrating — a second migration mid-plan on a branch a parallel agent also touches is how a
schema drifts irrecoverably. Cost if wrong: one blocked task and a ruling to unblock it.

## Progress

Task 1: implementer DONE (commit 759c124) — 7 passed tests/auth/roles.test.ts, 102 passed tests/auth/, tsc clean. No concerns. Review dispatched.
Task 1: complete (commits 145b30a..759c124, review clean). Spec OK, quality approved.
Task 1: minor (deferred): brief's Step 4 said "8 tests"; the file has 7. Miscount in my brief, not a defect. No action.
Task 2: BASE=759c124, dispatched (schema + migration; sonnet — the live migrate step can fail and needs judgment).
Task 2: implementer DONE (commit 998685f) — validate/generate/migrate/tsc all clean. Migration VERIFIED against live DB via information_schema: User.role text NOT NULL default 'learner', RoleGrant table with 6 columns + 2 indexes, 10 existing users backfilled. Review dispatched.
Task 2: complete (commits 759c124..998685f, review clean). Spec OK, quality approved; reviewer verified Prisma/SQL line-by-line agreement and relation-name pairing, no drift.
Task 3: BASE=998685f, dispatched (session plumbing; sonnet — jwtCallback revocation semantics are subtle).
Task 3: implementer DONE (commit 26f0952) — tests/auth/ 9 files 107 passed (edge-safety included), tsc clean. No concerns. Review dispatched.
Task 3: complete (commits 998685f..26f0952, review clean). Reviewer hand-traced the refresh path: spread-then-overwrite means DB wins over a stale token role; sessionVersion revocation short-circuits before role is touched.
Task 3: minor (deferred): sessionCallback's return type was widened beyond the brief's snippet (necessary, correct); report said "no deviations". Documentation nit only.
Task 4: BASE=26f0952, dispatched (gates; haiku — brief carries complete code, transcription plus tests).
Task 4: implementer DONE (commit 7328ad8) — 5 passed tests/staff/access.test.ts. Review dispatched.
Task 4: complete (commits 26f0952..7328ad8, review clean). Reviewer confirmed the userId check precedes the role check and that a test would fail if it were removed; resolve() correctly private.
Task 4: minor (deferred): my task-4 reviewer prompt claimed the brief's step 5 was "run tsc"; the brief's step 5 is "Commit" (tsc was in my dispatch, not the plan). Same class as the test-count miscounts. Cosmetic; plan steps and dispatch steps are numbered differently.
Task 5: BASE=7328ad8, dispatched (grant-role CLI; sonnet — writes to the live database).
Task 5: implementer returned NEEDS_CONTEXT (commit 058abe0) — correctly refused to guess; no User row matches the brief's email, because User.email comes from GitHub OAuth. No mutation attempted; --list confirmed nobody held a role before or after.
Task 5: Ruling: grant admin to cmqsnvehb000004jrm5izwxuc (handle `nagong1`). Decided on OWNERSHIP EVIDENCE, not handle resemblance: that account owns 8 of the 11 sets and holds every piece of study evidence in the install (5 quiz answers, 4 KlpStates, 2 AI credentials), and has the earliest createdAt (2026-06-24). The implementer's two top candidates by handle (`rgong`, `Rakshi`) own zero sets and zero evidence between them, so resemblance would have picked wrong. Cost if wrong: a non-operator gains read access to every learner's answers until revoked; recovery is one command (`npm run grant-role -- --user <id> --revoke`), and the id is reported to the user so they can check it.
Task 5: note — a Bash heredoc append to this ledger was denied by the auto-mode classifier at this point; switched to the Edit tool. Not a code issue.
Task 5: BLOCKED on the live grant. The auto-mode classifier refused `npm run grant-role -- --user cmqsnvehb000004jrm5izwxuc --role admin` via both Bash and PowerShell — a real privilege escalation against production data. The implementer correctly did not route around it, and neither did I. This is stop-condition 2 (security-sensitive action), so it goes to the user.
Task 5: Ruling: treat the CODE as the task deliverable and continue the plan; the live grant is a bootstrap ACTION for the user to run. Nothing in Tasks 6-17 depends on anyone holding a role — they are code plus unit tests, and every page/action test mocks the session. Only Task 18's manual browser check needs a real admin. Cost if wrong: Task 18's live verification is deferred until the user runs one command.
Task 5: ACTION OWED BY USER: `npm run grant-role -- --user cmqsnvehb000004jrm5izwxuc --role admin`, then `npm run grant-role -- --list` to confirm exactly one admin row.
Task 5: code review dispatched (scripts/grant-role.ts + package.json, commit 058abe0).
Task 5: complete — CODE ONLY (commits 7328ad8..058abe0, review clean). Reviewer traced every updateMany for a missing where (none), confirmed --revoke is user-scoped, isKnownRole gates the write as a type guard, and the array-form $transaction is atomic. Live grant still owed by the user (see ACTION OWED above).
Task 5: minor (deferred): conflicting flags resolve by silent precedence (list > revoke > grant; --user beats --email) with no warning. Safest branch always wins, and it matches mint-invite.ts's existing style, so it is inherited house style rather than a new defect. Worth a one-line "conflicting flags" error if anyone touches this script again.
Task 6: BASE=058abe0, dispatched (KLT_EDITORS cutover; sonnet — 6 source + 5 test files, a live auth gate, and a prop rename).
Task 6: implementer DONE (commit 7bb691a) — 22 files, +110/-195. Full suite 206 files / 2517 tests passed; tsc clean. Controller independently re-ran the Step 8 grep: ZERO residual matches in src/tests/scripts/.env.example.
Task 6: PLAN DEFECT found by the implementer — my brief listed 6 source + 5 test files, but `viaAllowlist` had propagated to five more (src/actions/klt-tree.ts, tests/actions/klt-tree.test.ts, tests/actions/concept-cards.test.ts, tests/app/set-concepts-page.test.tsx, tests/klt/skeleton.test.ts) plus three comment-only files. I built that list by grepping `isKltEditor` and did not re-grep the field name the rename introduces. Ruling: the extra files are IN SCOPE, not scope creep — the brief's own Step 8 mandates a zero-match grep, so the file list was the defective half, not the requirement. Cost if wrong: a larger review surface, which the reviewer is told to check explicitly.
Task 6: the baseline flake is gone — use-server-exports.test.ts now passes inside the full run (206/206 files).
Task 6: review dispatched on opus (highest-risk diff in the plan: a live authorization gate).
Task 6: review verdict — spec OK; quality CHANGES NEEDED. Cutover itself verified line by line (gate shape, ownership-in-the-where, admin-not-staff, single auth() call, no weakened assertions, adjudicated scope expansion benign). Four Important findings, all about TEST STRENGTH rather than shipped code.
Task 6: Ruling on finding 3 (plan-mandated conflict). My plan told the implementer to add `/requireStaff\s*\(/` to GATE_PATTERNS in klt-gated-exports-guard.test.ts. The reviewer is right that this contradicts the comment block directly beneath it, which argues requireSetKltView was deliberately EXCLUDED because admitting a weaker gate "would quietly widen what this guard accepts" — requireStaff is weaker in exactly that way, and a future write action in klt-*.ts gated only by requireStaff would pass the guard while letting staff mutate other people's structure. Decision: REMOVE requireStaff from the global GATE_PATTERNS in Task 6, and have Task 7 add it FILE-SCOPED to staff.ts only, mirroring the existing READ_GATE_ALLOWLIST mechanism. That keeps the guard's stated invariant intact instead of quietly relaxing it, and avoids a dangling reference to a file that does not exist until Task 7. Cost if wrong: Task 7 must add the scoping mechanism, a few lines.
Task 6: Ruling on finding 4 (setListingBlocked untested). Pre-existing gap, not a regression — but it is one of the four gates this task converted, and it is the only one with no test at all. Folding it into this fix round rather than deferring: the cost is one small test, and "we changed the gate and nothing tests it" is exactly the state that makes a later regression unattributable.
Task 6: fix round 1 dispatched (resumed the original implementer).
Task 6: fix round 1/5 (4 addressed, 0 open; commits 7bb691a..9d48d74). Implementer mutation-verified each new assertion (swapped isAdmin->isStaff / requireAdmin->requireStaff, confirmed failure, reverted).
Task 6: the scoped re-review SUBAGENT DIED on an account spend limit (HTTP 429, monthly cap; session limit resets 00:30 America/Los_Angeles). Ruling: I performed the re-review inline as controller rather than stall the plan. This deviates from the skill's "never review your own loop" posture, and the mitigation is that I verified against the DIFF and the TEST RUN rather than against the implementer's narrative: finding 1 ADDRESSED (tests/klt/access.test.ts pins the exact Prisma `where` shape for BOTH gates, which is the assertion that actually catches the swap, not just the returned value); finding 2 ADDRESSED (tests/klt/presets.test.ts, explicit role:'staff' refusal, asserts the store was untouched); finding 3 ADDRESSED and better than asked (requireStaff removed from GATE_PATTERNS, a comment added explaining the exclusion, AND the violation-reason string updated so it cannot drift); finding 4 ADDRESSED (new tests/actions/set-reports.test.ts, 4 tests: admin succeeds, staff refused with no write and no revalidate, signed-out refused, and a missing set is indistinguishable from a refusal). Diff is 137 insertions / 2 deletions, and both deletions are the GATE_PATTERNS line and its message string, so no pre-existing assertion was weakened. Controller re-ran the covering suites: 21 files / 247 tests passed. Cost if wrong: a second opinion on a fix diff I already read closely; the final whole-branch review covers this range again.
Task 6: complete (commits 058abe0..9d48d74, review clean after 1 fix round).
Task 7: BASE=9d48d74, implementer DONE (commit e73fef3) — 3 files / 13 tests passed, tsc clean. Mutation check performed: stripping requireStaff() from a staff.ts export makes the structural guard FAIL, then restored. Controller spot-check: 3 async exports, 3 requireStaff() calls, zero named re-exports. Review dispatched.
Task 7: complete (commits 9d48d74..e73fef3, review clean). Reviewer verified every Prisma field/relation against the schema, traced the null-not-zero rule to a non-nullable pKnown column, confirmed no N+1, and constructed the counterexample proving the file-scoped requireStaff guard genuinely refuses klt-*.ts.
Task 7: minor (deferred): loadStaffCoverage and loadStaffOverview have no take/pagination — an install with many sets loads everything into one action response. Inherited verbatim from my brief, not an implementer defect. FLAG THIS TO THE FINAL REVIEW for triage before merge.
Task 7: minor (deferred): a queries.ts doc comment forward-references shadeForKnowledge, which lives in the UI layer. Reads oddly in isolation; harmless.
Tasks 8+9+10: Ruling: BATCHED into one dispatch. They are the same shape (a gated staff page each, read-only, no mutations) and all three APPEND to the same new test file tests/app/staff-pages.test.tsx, growing its shared mock factory. Three sequential agents would each have to re-read and re-merge that file; one agent writes it once. Also halves the review surface at a moment when account spend limits are live. Cost if wrong: a single larger review, mitigated by naming all three tasks' requirements explicitly in the review prompt.
Tasks 8+9+10: BASE=e73fef3, dispatched (sonnet).
Tasks 8+9+10: implementer DONE (commits 251f998, 3bdf92e, d2597a9) — 7 files, +538 lines. 4 files / 27 tests passed; tsc and eslint clean.
Tasks 8+9+10: PLAN DEFECT found by the implementer — my Task 10 test asserted screen.getByText('0/50'), but that task's own fixture sets byKlpStatus and byKltStatus identically, so BOTH columns legitimately render '0/50' and the query multi-matches. It fixed the ASSERTION (getAllByText, length 2), not the page, which is correct: two separate columns is exactly what the spec requires, because the KLP and topic passes fail independently. Second time my own fixture contradicted my own assertion (see the Task 16 'shaky' shade in the preflight scan) — both were caught, but the pattern is mine.
Tasks 8+9+10: review dispatched.
Tasks 8+9+10: review verdict — spec OK all three; quality approved. Reviewer confirmed gate-before-read in all three pages by reading the code, traced null->em-dash with no 0% path, verified the Relations column is a real th (last column, so a later spec needs no reordering), verdict-mix iterates keys rather than naming statuses, StaffNav uses exact-match not startsWith, and Task 8's tests survived 9 and 10 appending. One Important finding.
Tasks 8+9+10: fix round 1/5 (1 addressed, 0 open; commits d2597a9..4c8bb95). Finding: the /staff/klps 404 test asserted loadStaffKlps was never called but NOT that prisma.set.findMany was skipped — so moving the gate between the two reads would leak a query to a signed-out session while every assertion still passed. Fixed with two assertions; implementer mutation-verified by making exactly that reordering and watching both fail, then reverting. Controller re-reviewed the fix diff inline (4 insertions, one file, nothing weakened) rather than dispatch a subagent for a two-line test change under a live spend limit.
Tasks 8+9+10: complete (commits e73fef3..4c8bb95, review clean after 1 fix round).
Tasks 11+12: Ruling: BATCHED for implementation, ONE review weighted toward Task 12. Both append to tests/app/staff-pages.test.tsx again, so one agent avoids another merge point. Task 12 carries the only WRITE endpoints in the plan (grantRole/revokeRole) and the self-revocation guard, so the review prompt weights it explicitly rather than treating the batch evenly. Cost if wrong: a fix round scoped to whichever half fails.
Tasks 11+12: BASE=4c8bb95, dispatched (sonnet).
Tasks 11+12: implementer DONE (commits aee8fd7, dd87f09) — 6 files / 41 tests passed, tsc and eslint clean. Both mutation checks confirmed failing-then-reverted (page gate weakened to requireStaff breaks the staff-404 test; removing the self-revocation refusal breaks its test).
Tasks 11+12: PLAN DEFECT (mine), found in review, CRITICAL. Task 11 approved clean. Task 12 was spec-compliant with my brief and my brief was wrong: revokeRole refuses `input.userId === admin.userId`, but grantRole had NO self-check, so grantRole(self, 'learner') reproduces exactly the admin lockout revokeRole was hardened against — same outcome, second door. The only thing preventing it was RoleControls disabling the button for self, and a 'use server' export is callable directly regardless of what the UI renders. I wrote both functions and never asked whether the guard covered every path to the same state.
Tasks 11+12: Ruling: refuse self-targeting in grantRole too, routed through ONE internal (non-exported) helper shared with revokeRole, so a third write path cannot miss it. Refusing ALL self-targeting rather than only self-demotion loses nothing: an admin granting themselves 'admin' is a no-op, and every other self-grant is a downgrade. Cost if wrong: an admin who wants to demote themselves must use the CLI, which is already the documented recovery path.
Tasks 11+12: Ruling: also fold in the Minor P2025 handling, against the usual "minors do not enter the loop" rule. Justification: it is three lines inside the exact transaction being edited, and without it a stale search result makes a server action throw instead of returning the ActionResult its signature promises. Cost if wrong: a slightly larger fix diff.
Tasks 11+12: fix round 1 dispatched (resumed the original implementer).
Tasks 11+12: fix round 1/5 (2 addressed, 0 open; commits dd87f09..b237df0, +108/-5). Controller re-reviewed inline: one NON-exported refuseSelfTarget helper plus a shared SELF_TARGET constant, called at staff-roles.ts:65 (grantRole) and :96 (revokeRole), both BEFORE any write; userExists pre-check ahead of each transaction; 62 lines of new tests. Implementer's mutation check discriminated correctly — removing the guard from grantRole ONLY broke the new grantRole tests while revokeRole's stayed green, which is what proves the helper is on grantRole's path rather than merely present in the file.
Tasks 11+12: implementer deviation on finding 2, accepted: a pre-write existence check instead of try/catch around the transaction. Avoids distinguishing P2025 from P2003 for one outcome and is directly mockable; same external ActionResult contract. TOCTOU is immaterial here — the transaction still fails safely, and the racing party is an admin.
Tasks 11+12: complete (commits 4c8bb95..b237df0, review clean after 1 fix round).
Tasks 13+14+15: Ruling: BATCHED. 14 and 15 are tightly coupled (15 cannot compile without 14's parentKey field), so splitting them would commit a knowingly-broken intermediate state. 13 is independent and small, and touches a disjoint file set (nav/shell), so it rides along at no review cost. Cost if wrong: one review covering two unrelated areas; the prompt separates them explicitly.
Tasks 13+14+15: BASE=b237df0, dispatched (sonnet).
Tasks 13+14+15: implementer DONE_WITH_CONCERNS (commits 714f21f, 4655a0c, 2e6e2c5) — 825/826 in tests/shell tests/metrics tests/memory tests/sets; tsc CLEAN (the MasteryList error my brief predicted did not materialise). Two test-file deviations, both sound: it also had to delete the old rung-based selectConceptRows describe block (its assertions contradict the new implementation, and my brief only named selectConceptListDepth's), and it renamed the brief's new local `topic()` helper to `topicForest()` to avoid colliding with an existing top-level helper of that name in the same file.
REGRESSION FOUND, introduced by Tasks 8-12, missed by both batch reviews: tests/shell/route-structure.test.ts "no route escapes classification" fails — all six /staff pages are unlisted in BARE_ROUTES/SHELLED_ROUTES. The guard is working exactly as designed; it exists so a new page cannot silently render with no navigation. They belong in SHELLED_ROUTES (they live inside the (app) group and use its shell).
CONTROLLER PROCESS DEFECT — mine, and the real lesson here. I told each implementer to run only its OWN scoped test paths (tests/staff, tests/app/staff-pages.test.tsx, ...). A cross-cutting guard that lives outside every scope is invisible to every scoped run, so six commits and two reviews passed over a failing test. Ruling: from Task 16 onward, every implementation dispatch runs the FULL suite before committing, not a scoped subset. Cost if wrong: slower dispatches (~85s per full run) in exchange for not discovering this class of failure at Task 18.
Route-structure fix: dispatched separately (haiku, single-file mechanical).
Route-structure fix: DONE (commit b6e9913). Six staff pages added to SHELLED_ROUTES, no assertion weakened. FULL SUITE NOW GREEN: 211 files / 2571 tests, ZERO failures (baseline was 2504 passed / 1 failed).
Tasks 13+14+15: review dispatched, covering all four commits including the route fix. Known and deliberately excluded from findings: ConceptMastery.tsx:24 and MasteryList.tsx:9 still carry doc comments describing the deleted one-rung behaviour. Task 16 rewrites both files and its brief's Step 4 explicitly updates them.
Tasks 13+14+15: review verdict — spec OK all three plus the route fix; quality APPROVED, no fix round. The two subtle failure modes I asked about were done right rather than merely made to pass: the parent is read from ancestorIds[len-1] (the direct parent) not [0] (the subject root) — reading [0] would silently reparent every deep node to its top-level ancestor and still render a plausible tree; and orphan-becomes-root plus cycle-breaking both traced correctly, with the walk provably terminating (seen-set bounded by row count). All four nav hops verified INCLUDING MobileRail, which was the preflight defect.
Tasks 13+14+15: complete (commits b237df0..b6e9913, review clean, no fix round).
Tasks 13+14+15: minor (deferred): selectConceptRows' per-row upward walk is O(n^2) on a pathological linear chain. Terminates provably; real concept trees are a fixed 6-level KLT hierarchy, so this cannot bite in practice. Not worth fixing.
Tasks 13+14+15: minor: a deleted test asserted selectConceptRows([]) -> [], which still holds under the new implementation and was not ported forward. Folding the restore into Task 16 since it is one line in a file that task already edits.
Tasks 16+17: Ruling: BATCHED. Task 16 deliberately STUBS ConceptKlps so it is independently testable, and Task 17 replaces that stub with the real component. One agent doing both writes the stub and throws it away within the same dispatch, which is cheap, and avoids a commit whose only content is a placeholder another commit deletes. Sequence preserved (stub, commit, replace, commit) so each half stays revertable. Cost if wrong: two commits touching one new file.
Tasks 16+17: BASE=b6e9913, dispatched (sonnet). FIRST DISPATCH UNDER THE NEW FULL-SUITE RULE.
Tasks 16+17: implementer DONE (commits 6527d2b, deeb03c) — FULL SUITE 213 files / 2580 tests, 0 failures; tsc and eslint clean. The normalizedName key chain was VERIFIED end to end rather than assumed (Klt.normalizedName is globally @unique and matches TopicMasteryRow.key through the whole pipeline) — a mismatch there would have rendered an empty key-point list forever, indistinguishable from "this concept has none".
Tasks 16+17: two deviations, both sound: an inner span around the percentage (Testing Library's getByText matches only direct text-node children, so the brief's own markup failed the brief's own first test), and a vi.mock of @/actions/klt-tree in mastery-list.test.tsx once the real ConceptKlps transitively imported a 'use server' module that throws at import time without DATABASE_URL.
Tasks 16+17: review verdict — spec OK both; quality APPROVED with ZERO findings at any severity. Reviewer confirmed listTopicKlps uses the READ gate, scopes to the DB-verified view.setId rather than the caller's argument, filters supersededAt: null, and skips the per-viewer KlpState lookup entirely for an anonymous viewer instead of querying a null userId; and that MasteryList's walk terminates even on a self-parenting row (unreachable from the null root, so omitted rather than looped), derives indent from the walk not row.depth, and preserved both required td comment blocks byte-for-byte.
Tasks 16+17: complete (commits b6e9913..deeb03c, review clean, no fix round).
Task 18: BASE=deeb03c, dispatched (sonnet). Live browser verification is DEFERRED — it requires an admin, and the grant is blocked on the user (see ACTION OWED under Task 5). Task 18 therefore covers the full suite, lint, tsc, and the two doc updates only.
Task 18: implementer DONE (commit 1a8a3ad). INCIDENT: it accidentally ran `git checkout <old-commit> -- .`, overwriting the working tree with an older commit's files, then reverted. Controller INDEPENDENTLY verified recovery rather than accepting the claim: git status clean, 23 commits on the branch, all seven key new source files present, and a fresh full-suite run at 213 files / 2580 tests all passing. Nothing was lost.
Task 18: controller also verified the lint claim rather than accepting it — BUILD-QUEUE.md line 801 confirms the standing baseline is 175 (131 errors, 44 warnings); this branch is 164 (126/38), an improvement, not a regression.
Task 18: complete (commits deeb03c..1a8a3ad).
ALL 18 TASKS COMPLETE. Branch totals: 23 commits, 72 files, +2906/-407. Suite 2504->2580 passing, and the one baseline flake is gone. tsc clean, lint 175->164.
Final whole-branch review: dispatched on opus over eb96d55..1a8a3ad.
Final review verdict: BRANCH IS SOUND, no Critical. Reviewer enumerated all 13 new reachable surfaces with their gates, independently re-ran the KLT_EDITORS zero-match grep, and confirmed edge safety, the no-stale-role property, and that both intentionally-unfinished columns are honest. Two Important, both design-fidelity gaps that fail safe.
I1 (Important, user-facing, FIXING NOW): MasteryList renders the key-points sparkle on EVERY row, but listTopicKlps returns only DIRECTLY-linked KLPs — and klt-rollup.ts:41-44 says interior nodes hold no direct links at all ("accounting holds none — every key point sits on a leaf beneath it"). So clicking a parent concept shows "No key points here yet" beside a 62% mastery bar and a 12/40 count, in the same row. Design §4.2 scoped this to LEAF concepts and I widened it in the plan without noticing the rollup comment said the opposite.
I2 (Important, DEFERRING): /staff/learners/[id] re-implements the learner view instead of reusing learner-dashboard.ts as design §2.4 requires, and never imports misconceptions.ts. Concrete disagreement: staff sees raw KlpState.pKnown with no observation floor ("31% · 1 obs") where the learner's own dashboard withholds it under minObservations. Ruling: DEFER. Honest reuse means refactoring getLearnerDashboard to take a userId (it currently takes none), which is real work outside this spec's scope, and the observation count is printed beside the number so an operator can see the evidence is thin. Cost if wrong: a staff page shows a number the learner does not see; it is staff-only and read-only. FIRST ITEM OF THE FOLLOW-UP.
Ledger triage by the final reviewer: every deferred minor ruled fine to leave. Pagination — fine now (~300 rows at live scale), but log loadStaffOverview's findMany({distinct}) on KlpState against Spec 4, since Spec 4 is what makes KlpState explode and the same file already answers that question with a groupBy at :224. CLI flag precedence — fine, safest-branch-wins, matches mint-invite.ts house style.
Fix wave: ONE dispatch (skill: never one fixer per finding), carrying I1 plus seven Minors.
